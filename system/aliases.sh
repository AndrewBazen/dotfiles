# Custom Aliases

# Shortcuts
alias ll='ls -lah'
alias gs='git status'
alias gc='git commit'
alias gp='git push'
alias gl='git pull'
alias vim='nvim'
alias vi='nvim'
alias ..='cd ..'
alias ...='cd ../..'

# System info
alias update='sudo apt update && sudo apt upgrade -y'
alias weather='curl wttr.in'

# Safety
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'
